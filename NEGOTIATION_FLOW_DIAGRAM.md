# 🔄 Price Negotiation Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         PRICE NEGOTIATION WORKFLOW                           │
└─────────────────────────────────────────────────────────────────────────────┘

STEP 1: Customer Makes Custom Request
════════════════════════════════════════
┌─────────────┐
│  Customer   │  Creates custom order/request
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Artisan    │  Receives notification: "New custom request"
└──────┬──────┘
       │
       ▼


STEP 2: Artisan Proposes Initial Price
═══════════════════════════════════════
┌─────────────┐
│  Artisan    │  Sets price (e.g., $200)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Customer   │  Receives notification: "Price proposed: $200"
└──────┬──────┘
       │
       ▼


STEP 3: Customer Negotiates (Says Price Too High)
══════════════════════════════════════════════════
┌─────────────┐
│  Customer   │  Clicks "💬 Negotiate"
└──────┬──────┘  Types: "Price is too high, can you reduce?"
       │
       ▼
┌─────────────────────────────────────────┐
│  Backend creates negotiation             │
│  - Status: "Negotiating"                 │
│  - Sends notification to artisan         │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────┐
│  Artisan    │  ✨ NEW FEATURE: Sees notification with:
└─────────────┘  - 💰 Icon
                 - Customer message
                 - Two action buttons:
                   [💰 Update Price]  [❌ Reject]


STEP 4A: Artisan Updates Price ⭐ NEW
═══════════════════════════════════════
┌─────────────┐
│  Artisan    │  1. Clicks "💰 Update Price"
└──────┬──────┘  2. Form appears
       │         3. Enters new price: $150
       │         4. Clicks "✓ Submit"
       │
       ▼
┌─────────────────────────────────────────┐
│  PUT /api/reservations/:id/update-price  │
│  Body: { agreed_price: 150 }             │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Backend:                                 │
│  - Updates agreed_price to $150          │
│  - Status: "Price_Proposed"              │
│  - Creates notification for customer     │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────┐
│  Customer   │  Receives: "Artisan updated price to $150"
└──────┬──────┘  Can now: Accept/Negotiate again/Reject
       │
       ▼
   [Cycle can repeat until agreement]


STEP 4B: Artisan Rejects ⭐ NEW
═══════════════════════════════
┌─────────────┐
│  Artisan    │  1. Clicks "❌ Reject"
└──────┬──────┘  2. Confirms in dialog
       │
       ▼
┌──────────────────────────────────────────────┐
│  PUT /api/reservations/:id/reject-negotiation│
└──────┬───────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Backend:                                 │
│  - Status: "Rejected"                    │
│  - Creates notification for customer     │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────┐
│  Customer   │  Receives: "Artisan declined negotiation"
└─────────────┘  Reservation closed


═══════════════════════════════════════════════════════════════
                    COMPONENT INTERACTION
═══════════════════════════════════════════════════════════════

NotificationCard.jsx
├── Detects negotiation notification
├── Shows action buttons
├── handleUpdatePrice()
│   ├── Validates input
│   ├── Calls onPriceUpdate()
│   └── Shows loading state
└── handleReject()
    ├── Shows confirmation
    ├── Calls onReject()
    └── Shows loading state
         │
         ▼
Notifications.jsx
├── handlePriceUpdate(reservationId, newPrice)
│   └── Calls NotificationController.updateNegotiationPrice()
└── handleRejectNegotiation(reservationId)
    └── Calls NotificationController.rejectNegotiation()
         │
         ▼
NotificationController.js
├── updateNegotiationPrice()
│   └── Calls notificationService.updateNegotiationPrice()
└── rejectNegotiation()
    └── Calls notificationService.rejectNegotiation()
         │
         ▼
notificationService.js
├── updateNegotiationPrice()
│   └── PUT /api/reservations/:id/update-price
└── rejectNegotiation()
    └── PUT /api/reservations/:id/reject-negotiation
         │
         ▼
    Backend API
    (Needs implementation)


═══════════════════════════════════════════════════════════════
                    UI STATES & TRANSITIONS
═══════════════════════════════════════════════════════════════

Initial Notification View:
┌─────────────────────────────────────┐
│ 💰 Negotiation    2h ago            │
│                                      │
│ "Price is too high, can you reduce?"│
│                                      │
│ [💰 Update Price]  [❌ Reject]      │
└─────────────────────────────────────┘

After Clicking "Update Price":
┌─────────────────────────────────────┐
│ 💰 Negotiation    2h ago            │
│                                      │
│ "Price is too high, can you reduce?"│
│                                      │
│ ┌─────────────────────────────────┐ │
│ │ New Price ($):                  │ │
│ │ [   150.00   ] [✓Submit][Cancel]│ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘

During Processing:
┌─────────────────────────────────────┐
│ 💰 Negotiation    2h ago            │
│                                      │
│ "Price is too high, can you reduce?"│
│                                      │
│ [⏳ Updating...]                     │
└─────────────────────────────────────┘

After Success:
┌─────────────────────────────────────┐
│ ✅ Alert: "Price updated successfully!│
│    Customer has been notified."      │
└─────────────────────────────────────┘
[Notification auto-refreshes]


═══════════════════════════════════════════════════════════════
                    KEY FEATURES SUMMARY
═══════════════════════════════════════════════════════════════

✅ Inline price editing
✅ Rejection with confirmation
✅ Real-time validation
✅ Loading states
✅ Success/error messages
✅ Auto-refresh after actions
✅ Mobile responsive
✅ Error handling
✅ Disabled states during API calls
✅ User-friendly UI/UX

═══════════════════════════════════════════════════════════════
```

## Status Transitions

```
Customer Action      →   Status          →   Artisan Action     →   New Status
─────────────────────────────────────────────────────────────────────────────────
Creates request      →   "New"           →   Proposes price     →   "Price_Proposed"
Sees price          →   "Price_Proposed" →   -                  →   -
Negotiates          →   "Negotiating"    →   Updates price      →   "Price_Proposed"
                     │                   →   Rejects            →   "Rejected"
                     │
Accepts price       →   "Price_Proposed" →   -                  →   "Accepted"
Rejects price       →   "Price_Proposed" →   -                  →   "Rejected"
```

## Data Flow

```
Frontend (React)                Backend (Express)                Database (MongoDB)
─────────────────              ─────────────────               ─────────────────

NotificationCard
      ↓
Notifications.jsx
      ↓
NotificationController
      ↓
notificationService
      ↓
API Request (PUT) ──────────→  Route Handler ───────────────→  Update Reservation
                                     ↓                               ↓
                               Create Notification                Save Changes
                                     ↓                               ↓
API Response ←──────────────  Send Response ←──────────────  Return Updated Doc
      ↓
Success Alert
      ↓
Auto-refresh
      ↓
Updated UI
```

---

**🎉 Implementation Complete!**
All components are connected and ready to handle price negotiations.
